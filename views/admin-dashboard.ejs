<%- include('partials/header') %><section><h2 style='text-align:center'>Admin Dashboard</h2><a class='btn secondary' href='/admin-logout'>Logout</a><div style='margin-top:20px'> <% if (bookings.length===0){ %><p>No bookings yet.</p><% } %> <% bookings.forEach(function(b){ %><div class='card' style='display:flex;justify-content:space-between;align-items:center;margin-bottom:10px'><div><strong><%= b.name %></strong> — <%= b.pickup %> → <%= b.dropoff %> — <%= b.date %> — <em><%= b.status %></em><div>ID: <%= b.id %></div></div><div><form action='/admin/confirm/<%= b.id %>' method='post' style='display:inline'><button class='btn' type='submit'>Confirm</button></form> <form action='/admin/cancel/<%= b.id %>' method='post' style='display:inline'><button class='btn btn-danger' type='submit'>Cancel</button></form></div></div><% }); %></div></section><%- include('partials/footer') %>